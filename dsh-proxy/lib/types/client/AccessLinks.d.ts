/** Render exactly the addresses reported by the Host; no credentials are encoded. */
export declare function AccessLinks({ urls, errorText }: {
    urls: readonly string[];
    errorText: string;
}): import("react").JSX.Element;
